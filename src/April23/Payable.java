package April23;

public interface Payable {
    void pay(double amount);
    void refund (double amount);
}

//interface is used to make sure to check "must" CheckClient all the 500 methods not 492

//interface is the best practice used for big use
interface CheckClient{
    500
}
interface Approve{
    100
}

class Bank implements CheckClient,Approve{
    800 methods

}

class Insurence  implements CheckClient{
    2300 methods
    -500
    -100
}

class Gov implements CheckClient {
    12000 methods
}